# Where the Next.js server proxies /api/* (server-side rewrite)
BACKEND_URL=http://localhost:8000

# WebSocket URL the BROWSER connects to (must be wss:// in production)
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws
