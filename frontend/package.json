{
  "name": "cloud-ai-trader",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "build:static": "BUILD_TARGET=static next build",
    "icons": "node scripts/gen-icons.mjs",
    "cap:sync": "npm run build:static && npx cap sync",
    "cap:android": "npm run build:static && npx cap sync android && npx cap open android",
    "cap:ios": "npm run build:static && npx cap sync ios && npx cap open ios",
    "desktop:dev": "ELECTRON_DEV=1 electron electron/main.js",
    "desktop:build": "npm run build:static && electron-builder",
    "desktop:win": "npm run build:static && electron-builder --win",
    "desktop:mac": "npm run build:static && electron-builder --mac",
    "prebuild": "node scripts/stamp-version.mjs"
  },
  "dependencies": {
    "lightweight-charts": "^4.2.3",
    "next": "^15.1.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "sharp": "^0.35.1",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.2"
  },
  "comment:optionalPackaging": "Install these only on the build machine for native targets \u2014 kept out of the core install so the web build stays lean. npm i -D @capacitor/cli @capacitor/core @capacitor/android @capacitor/ios electron electron-builder electron-updater",
  "optionalDependencies": {}
}
