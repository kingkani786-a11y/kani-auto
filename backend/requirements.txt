fastapi>=0.115
uvicorn[standard]>=0.34
httpx>=0.28
pydantic>=2.12
pydantic-settings>=2.7
python-dotenv>=1.0
supabase>=2.11
numpy>=2.2

# AI Cortex (Proposal #013) — OPTIONAL. Install only the provider you use.
# The backend runs fine without either; adapters are lazy-imported.
# google-genai>=1.0          # for CAT_AI_PROVIDER=gemini
# anthropic>=0.40            # for CAT_AI_PROVIDER=anthropic
